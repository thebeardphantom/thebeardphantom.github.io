{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "jekyll",
            "command": "bundle exec jekyll serve",
            "type": "shell",
            "isBackground": true,
            "options": {
                "cwd": "${workspaceFolder}\\docs"
            },
        }
    ]
}